datasets>=2.19.0
huggingface-hub>=0.23.0
numpy>=1.24.0
pandas>=2.0.0
pillow>=10.0.0
pyarrow>=15.0.0
pyyaml>=6.0.0
requests>=2.31.0
scikit-learn>=1.3.0
tabulate>=0.9.0
tqdm>=4.66.0
transformers>=4.49.0
accelerate>=0.30.0
qwen-vl-utils>=0.0.8

# Optional runtime extras:
# vllm>=0.8.0        # fast multimodal inference
# torch              # install the CUDA build matching your environment
# wandb              # experiment tracking
# verl               # GRPO/RL training backend
